---
title: 简单的学生数据处理
date: 2024-01-06 11:57:52
tags: [Python, pandas, 数据分析]
categories: 数据分析
---

## 简单的学生数据处理

### 对学生数据预处理

```python
#对学生出现某列名相同如出现x.1 进行处理
data_frame=pd.read_excel(r'D:\17uml成绩.xls',header=5, nrows=1).values[0]
df=pd.read_excel(r'D:\17uml成绩.xls',header=5, skiprows=1)
df.columns=data_frame
```

### 简单对数据分区间并画图

```python
age_bins = [0,60,70,80,90,100]
d5= pd.concat([d4,d2],ignore_index=True) #两个列表所相加
a_bins=[0,60,70,80,90,100]
d5["区间"]= pd.cut(d5["总评成绩"],bins=a_bins) #产生一个区间
counts=pd.value_counts(d5["区间"],sort=False)
##下面可以取消注释进行画图
#树状图有用可以画出
# b=plt.bar(counts.index.astype(str),counts)
# plt.bar_label(b,counts)
# d5.plot.bar(d5["区间"],color="orange",edgecolor="green",width=0.6)
# plt.title("学生的成绩区间分布图")
# plt.show()
```

### 完整代码

```python
import pandas as pd
import xlrd
import matplotlib.pyplot as plt

plt.rcParams['font.sans-serif'] = ['SimHei']   #解决中文显示问题
plt.rcParams['axes.unicode_minus'] = False    # 解决中文显示问题

#这个注释是一个示例，为后面将excel表进行纵向排列准备
# data_frame=pd.read_excel(r'D:\17uml成绩.xls',header=5, nrows=1).values[0]
# df=pd.read_excel(r'D:\17uml成绩.xls',header=5, skiprows=1)
# df.columns=data_frame
d1=pd.read_excel(r'D:\17uml成绩.xls',header=5,usecols=[6,7,8,9,10,11],nrows=1).values[0]
d2=pd.read_excel(r'D:\17uml成绩.xls',header=5, usecols=[6,7,8,9,10,11],skiprows=1)  #得到右边那一部分
d3=pd.read_excel(r'D:\17uml成绩.xls',header=5,usecols=[0,1,2,3,4,5],nrows=1).values[0]
d4=pd.read_excel(r'D:\17uml成绩.xls',header=5, usecols=[0,1,2,3,4,5],skiprows=1)  #得到左边那一部分 可以单独输出试一下
d4.columns=d3
d2.columns=d1
age_bins = [0,60,70,80,90,100]
d5= pd.concat([d4,d2],ignore_index=True) #两个列表所相加
a_bins=[0,60,70,80,90,100]
d5["区间"]= pd.cut(d5["总评成绩"],bins=a_bins) #产生一个区间
counts=pd.value_counts(d5["区间"],sort=False)

#树状图有用可以画出
# b=plt.bar(counts.index.astype(str),counts)
# plt.bar_label(b,counts)
# d5.plot.bar(d5["区间"],color="orange",edgecolor="green",width=0.6)
# plt.title("学生的成绩区间分布图")
# plt.show()

#画饼图 答案报错，明天修改
# plt.pie(d5["区间"],labels=counts,autopct='%3.1f%%')
#
# plt.show()
# print(d5)
#平时成绩  考试成绩  总评成绩
#计算各类成绩平均值、最高分、最低分
print(d5.agg({"平时成绩":['mean','max','min'],"考试成绩":['mean','max','min'],"总评成绩":['mean','max','min']})) #简单使用得出结果 可以上网搜索agg的使用方法
```

-   **

